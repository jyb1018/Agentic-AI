[project]
name = "sesac-ai-practice"
version = "0.1.0"
description = "Agentic AI 실습 환경 (11일 과정)"
requires-python = "==3.13.*"
dependencies = [
    # --- LangChain / LangGraph 계열 (강사 환경 kt10 실측 버전 고정) ---
    "langchain==1.3.15",
    "langchain-core==1.6.0",
    "langchain-openai==1.5.2",
    "langchain-chroma==1.1.0",
    "langchain-mcp-adapters==0.3.2",
    "langgraph==1.2.11",
    "langgraph-checkpoint-sqlite==3.1.1",
    "langsmith==0.11.1",
    # --- 모델 호출 ---
    "litellm==1.97.0",
    "langchain-litellm==0.7.0",
    "langchain-community==0.4.2",
    "openai==2.54.0",
    "google-genai==2.20.0",
    # --- MCP ---
    "mcp==1.29.1",
    # --- 서비스화 (fastapi dev CLI 포함 — standard 필수) ---
    "fastapi[standard]==0.141.1",
    "pydantic==2.13.4",
    # --- 검색·문서·유틸 ---
    "chromadb==1.5.9",
    "tiktoken==0.14.0",
    "python-dotenv==1.2.3",
    # --- 아래 4종은 강사 환경 kt10에 없어 이번에 새로 넣은 것 (2026-09-03 해석 결과 고정) ---
    "pandas==3.0.5",
    "pypdf==6.16.2",
    "jupyterlab==4.6.3",
    "ipykernel==7.3.0",
]

[tool.uv]
package = false
